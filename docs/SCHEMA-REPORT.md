# Structured Data Validation

Validated: 2026-07-30

## Scope

Validation ran against the JSON-LD embedded in the production HTML generated by Astro for four representative page types.

| Page type | Representative route | Detected page-specific types | Result |
| --- | --- | --- | --- |
| Home | `/` | `Person`, `ProfessionalService`, `WebSite` | Pass |
| Service | `/criacao-de-sites/` | `BreadcrumbList`, `Service`, `FAQPage` | Pass |
| Portfolio case | `/portfolio/tresor-incorporacoes/` | `BreadcrumbList`, `CreativeWork` | Pass |
| Article | `/insights/checklist-lancamento-site-empresarial/` | `BreadcrumbList`, `BlogPosting`, `FAQPage` | Pass |

## Checks performed

- JSON-LD exists in the server-rendered HTML.
- Each JSON payload parses successfully.
- The top-level Schema.org context is present.
- Expected schema types exist for each page template.
- URL-like properties use absolute HTTPS URLs.
- No placeholder values were detected in the representative graphs.
- No deprecated schema type was detected.

## Advisory

- `FAQPage` remains technically valid and mirrors visible content, but Google no longer provides FAQ rich-result benefits for this commercial site. It is not counted as a ranking or rich-result advantage.
- This is rendered-source validation. After the changes are deployed, run one live representative URL through Google's Rich Results Test to confirm Google's fetch and interpretation in production.

No schema changes were required.
